# Task PRD-0205: Build Attack Optimizer Loop

> PRD: PRD-02 | Depends: PRD-0204
> Files: 3 | Lines: ~240 | Time: ~75min

## What to Build
- Implement the attack loop over rendered batches.
- Add checkpointing, metric logging, and early-exit hooks for dry runs.

## Acceptance Criteria
- [ ] A short fixture run decreases total loss across steps.
- [ ] Optimizer updates only `sh0`.
- [ ] Checkpoints include config snapshot and step metadata.

## Implementation Notes
- Keep logging structured so PRD-04 can reuse it for evaluation reports.

## Commands
```bash
uv run pytest tests/test_core_attack.py -k optimizer -v
uv run python -m anima_def_rpga.attack.optimizer --config configs/attack.toml --dry-run
```
