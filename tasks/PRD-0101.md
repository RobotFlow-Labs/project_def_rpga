# Task PRD-0101: Normalize Module Identity

> PRD: PRD-01 | Depends: None
> Files: 5 | Lines: ~220 | Time: ~60min

## What to Build
- Introduce `src/anima_def_rpga/` as the canonical package root.
- Add settings fields for `module_name`, `paper_arxiv`, `package_name`, and upstream repo URLs.
- Add a temporary compatibility note for stale `anima_hachiman` scaffold code.

## Acceptance Criteria
- [ ] New code imports from `anima_def_rpga`.
- [ ] Settings serialize the corrected paper id `2507.01367`.
- [ ] Tests fail if new config still references `2503.16190` or `HACHIMAN`.

## Implementation Notes
- Do not delete scaffold files yet; isolate the rename at the config and package boundary first.
- Use this task to establish the canonical constants consumed by later tasks.

## Commands
```bash
uv run pytest tests/test_foundation_config.py -k identity -v
uv run ruff check src/ tests/
```
