# Task PRD-0204: Add Min-Max Background and Physical Losses

> PRD: PRD-02 | Depends: PRD-0203
> Files: 4 | Lines: ~260 | Time: ~90min

## What to Build
- Implement the `sigma` max-step with `L_inf` clipping.
- Add EoT, NPS, and primary-color losses.
- Expose all weights in `configs/attack.toml`.

## Acceptance Criteria
- [ ] `sigma` is constrained outside the target mask only.
- [ ] NPS accepts a printable palette and returns scalar loss.
- [ ] Color regularizer uses background-derived palette centroids.

## Implementation Notes
- The paper leaves some exact hyperparameters implicit; start with config-driven defaults and keep every weight user-visible.

## Commands
```bash
uv run pytest tests/test_core_attack.py -k "minmax or nps or color" -v
```
