# Task PRD-0202: Add Masking and Composition

> PRD: PRD-02 | Depends: PRD-0201
> Files: 3 | Lines: ~180 | Time: ~60min

## What to Build
- Implement target-mask loading and optional SAM-backed mask inference.
- Compose adversarial foreground with clean background exactly as the paper’s Eq. (4) requires.

## Acceptance Criteria
- [ ] Mask tensor shape is `[B,1,H,W]`.
- [ ] Background pixels remain untouched outside the mask.
- [ ] Tests cover mask-file path and prompt-backed path.

## Implementation Notes
- Keep SAM optional so unit tests can run with precomputed masks.

## Commands
```bash
uv run pytest tests/test_core_attack.py -k mask -v
```
