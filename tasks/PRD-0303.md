# Task PRD-0303: Add Detector Adapter

> PRD: PRD-03 | Depends: PRD-0302
> Files: 3 | Lines: ~220 | Time: ~75min

## What to Build
- Implement MMDetection-backed detector wrappers with a common output type.
- Start with Faster R-CNN, then make YOLO-v5 / Mask R-CNN / Deformable-DETR pluggable.

## Acceptance Criteria
- [ ] Faster R-CNN path is testable with mocked outputs.
- [ ] Adapter returns boxes, labels, scores, and optional loss terms.
- [ ] Detector config is externalized in config files.

## Commands
```bash
uv run pytest tests/test_inference_pipeline.py -k detector -v
```
