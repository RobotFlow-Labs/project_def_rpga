# Task PRD-0103: Build Asset Audit CLI

> PRD: PRD-01 | Depends: PRD-0102
> Files: 3 | Lines: ~160 | Time: ~45min

## What to Build
- Implement `check_assets` CLI for paper PDF, detector weights, datasets, and scene checkpoints.
- Make the CLI flag the stale local paper artifact explicitly.

## Acceptance Criteria
- [ ] CLI returns non-zero on missing mandatory assets.
- [ ] CLI prints a warning for `papers/2503.16190_PGA-Camouflage.pdf`.
- [ ] Snapshot tests cover successful and failing runs.

## Implementation Notes
- This command becomes the preflight gate before attack or evaluation jobs.

## Commands
```bash
uv run python -m anima_def_rpga.cli.check_assets --config configs/foundation.toml
uv run pytest tests/test_foundation_config.py -k assets -v
```
