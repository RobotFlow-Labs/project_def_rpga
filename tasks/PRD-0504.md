# Task PRD-0504: Add Docker Runtime

> PRD: PRD-05 | Depends: PRD-0503
> Files: 3 | Lines: ~180 | Time: ~60min

## What to Build
- Create CUDA-first Dockerfile, compose file, and env template.
- Mount datasets, checkpoints, and export directories predictably.

## Acceptance Criteria
- [ ] `docker compose config` passes.
- [ ] Container startup command serves the API.
- [ ] Healthcheck script can run without a full benchmark job.

## Commands
```bash
docker compose -f docker/docker-compose.yml config
uv run pytest tests/test_api_smoke.py -k docker -v
```
