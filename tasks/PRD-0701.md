# Task PRD-0701: Emit Provenance Bundles

> PRD: PRD-07 | Depends: PRD-0604
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build
- Collect config snapshot, git state, asset hashes, and detector identifiers into a run provenance record.
- Attach provenance to all optimize/evaluate/export outputs.

## Acceptance Criteria
- [ ] Provenance bundle is emitted for every mocked run.
- [ ] Hashing skips large binary artifacts when configured.
- [ ] Tests validate required provenance keys.

## Commands
```bash
uv run pytest tests/test_production_validation.py -k provenance -v
```
