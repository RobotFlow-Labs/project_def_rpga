# Task PRD-0201: Implement Gaussian Scene State

> PRD: PRD-02 | Depends: PRD-0104
> Files: 3 | Lines: ~220 | Time: ~75min

## What to Build
- Create `GaussianState` with tensors for position, scale, rotation, opacity, `sh0`, and `sh_rest`.
- Add load / save adapters for upstream-style checkpoint payloads.

## Acceptance Criteria
- [ ] Loader handles frozen higher-order SH coefficients separately from `sh0`.
- [ ] Device transfer and dtype conversion are explicit.
- [ ] Fixture checkpoint can be loaded into a valid `GaussianState`.

## Implementation Notes
- This task is the boundary between repo-native code and upstream scene artifacts.

## Commands
```bash
uv run pytest tests/test_core_attack.py -k gaussian -v
```
