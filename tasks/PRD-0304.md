# Task PRD-0304: Export Camouflage Artifacts

> PRD: PRD-03 | Depends: PRD-0303
> Files: 3 | Lines: ~180 | Time: ~60min

## What to Build
- Export printable sticker sheets, texture payloads, and scene-linked metadata.
- Add CLI commands for render preview, detector scoring, and export.

## Acceptance Criteria
- [ ] Export bundle contains image assets plus JSON metadata.
- [ ] CLI exposes `render`, `score`, and `export`.
- [ ] Tests cover output manifest shape.

## Commands
```bash
uv run pytest tests/test_inference_pipeline.py -k export -v
uv run python -m anima_def_rpga.cli.infer export --dry-run
```
