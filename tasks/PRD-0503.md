# Task PRD-0503: Build FastAPI Surface

> PRD: PRD-05 | Depends: PRD-0502
> Files: 2 | Lines: ~160 | Time: ~60min

## What to Build
- Wire the FastAPI app, endpoint handlers, and health route.
- Mount inference/evaluation/export job entrypoints.

## Acceptance Criteria
- [ ] `/health` returns dependency and asset status summary.
- [ ] `/optimize`, `/evaluate`, and `/export` return job handles.
- [ ] API tests pass under `TestClient`.

## Commands
```bash
uv run pytest tests/test_api_smoke.py -k app -v
```
